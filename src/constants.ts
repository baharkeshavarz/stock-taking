export const DEFAULT_SCAN_CODE_ROUTE = "/scan-code";
export const DEFAULT_STOCK_KEEPER_ROUTE = "/start";
export const DEFAULT_ITEMS_ROUTE = "/products";
export const DEFAULT_START_ROUTE = "/create_user";
export const DEFAULT_SETTING_ROUTE = "/settings";

export const DEFAULT_LOGO_PATH = "/logo.png";

export const defaultScannerConstraints = {
  width: 1920,
  height: 1080,
  aspectRatio: 1,
};

export const defaultScannerLocatorSettings = {
  patchSize: "medium",
  halfSample: true,
};
